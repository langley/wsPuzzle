# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
# GET     /                           controllers.Application.index

# Test points for WsPuzzle just hit "/" and you'll see intermitent failures
GET		/							controllers.WsPuzzle.getPostEndpoint(remoteUrl="http://localhost:9000/postTestEndpoint")
GET		/stream						controllers.WsPuzzle.getPostEndpoint(remoteUrl="http://localhost:9000/postTestEndpointWithStream")
POST	/wsPostWithIterators		controllers.WsPuzzle.wsPostWithIterators(remoteUrl="http://localhost:9000/postTestEndpoint")
POST	/postTestEndpointWithStream  controllers.WsPuzzle.wsPostWithIteratorsWithStream(remoteUrl="http://localhost:9000/postTestEndpoint")
POST	/postTestEndpoint			controllers.WsPuzzle.postTestEndpoint

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
